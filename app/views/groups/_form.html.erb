<%= form_for([@client, @group]) do |f| %>
  <fieldset>
    <%= render "shared/errors", object: @group %>
    <p>
      <%= f.label :name %><br>
      <%= f.text_field :name, :autofocus => true, :required => true %>
    </p>
    <p>
      <%= f.submit %>
    </p>
  </fieldset>
<% end %>
